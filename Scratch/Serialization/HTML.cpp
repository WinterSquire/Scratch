#include <iostream>

#include "HTML.hpp"
#include "../Scratch.hpp"

int CHTMLSerializer::process(
    const uint64_t* timestampList, 
    const struct ScratchResultKinetic& data, 
    size_t size, 
    std::ostream& os)
{
    return 0;
}
